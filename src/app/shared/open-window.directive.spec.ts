import { OpenWindowDirective } from './open-window.directive';

describe('OpenWindowDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenWindowDirective();
    expect(directive).toBeTruthy();
  });
});
