import { DiscountFormatPipe } from './discount-format.pipe';

describe('DiscountFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DiscountFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
